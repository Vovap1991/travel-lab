import styled from 'styled-components';

export const SecondaryButton = styled.button`
  padding: 10px;
  border-radius: 15px;
  width: 170px;
  border-color: var(--grey-gold);
  color: var(--grey-gold);
  outline: none;
  font-size: 15px;
`;
